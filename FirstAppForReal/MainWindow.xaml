<Window x:Class="FirstAppForReal.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FirstAppForReal"
        mc:Ignorable="d"
        Title="Benjamin Franklin" Height="200" Width="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label FontSize="50" Grid.Row="0" Grid.Column="0">Names</Label>
        <ListBox Grid.Row="1" Grid.Column="0" x:Name="lstNames"/>
        <StackPanel Grid.Row="1" Grid.Column="1" Margin="5,0,0,0">
            <TextBox x:Name="txtName"/>
            <Button Content="Add Name" x:Name="btnAdd" Margin="0,5,0,0" Click="ButtonAddName_Click"/>

        </StackPanel>
    </Grid>
</Window>
